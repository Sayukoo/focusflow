# Optional: used for automatic music category labels in the player.
# Keep the real key in .env.local; do not commit it.
# In a distributed app, use a backend proxy because Vite bundles this key.
VITE_GEMINI_API_KEY=
VITE_GEMINI_MODEL=gemini-3.1-flash-lite
